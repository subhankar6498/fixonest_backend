<div
  style="height: 100vh"
  class="container d-flex justify-content-center align-items-center"
>
  <div class="col-md-12 shadow-lg p-3 bg-body rounded">
    <!-- Display success message if available -->
    <% if (successMessage && successMessage.length > 0) { %>
    <div class="alert alert-success mt-3" role="alert">
      <%= successMessage %>
    </div>
    <% } %>

    <!-- Display error message if available -->
    <% if (errorMessage && errorMessage.length > 0) { %>
    <div class="alert alert-danger mt-3" role="alert"><%= errorMessage %></div>
    <% } %>

    <h2 class="text-center mb-4">Create Testimonial</h2>
    <div class="card-body p-1">
      <form
        action="<%= '/admin' + generateUrl('testimonial-create') %>"
        method="POST"
        enctype="multipart/form-data"
      >
        <div class="row">
          <!-- Column 1: Personal Information -->
          <div class="col-md-12">
            <div class="card h-100">
              <div class="card-body">
                <!-- Personal Information Fields -->
                <div class="mb-3">
                  <label for="clientName" class="form-label">Client Name</label>
                  <input
                    type="text"
                    class="form-control"
                    id="clientName"
                    name="clientName"
                    placeholder="Enter client name"
                    required
                  />
                </div>
                <div class="mb-3">
                  <label for="testimonialImage" class="form-label">Image</label>
                  <input
                    type="file"
                    class="form-control"
                    id="testimonialImage"
                    name="testimonialImage"
                    accept="image/*"
                    required
                    onchange="previewImage(event)"
                  />
                  <div class="form-text">
                    Supported: JPG, PNG, JPEG (Max: 2MB)
                  </div>
                  <div class="mt-2">
                    <img
                      id="imagePreview"
                      src=""
                      alt="Image Preview"
                      class="img-fluid rounded"
                      style="
                        display: none;
                        max-width: 150px;
                        max-height: 150px;
                        border: 1px solid #ddd;
                      "
                    />
                  </div>
                </div>

                <div class="mb-3">
                  <label for="talk" class="form-label">Talk</label>
                  <textarea
                    type="text"
                    rows="4"
                    class="form-control"
                    id="talk"
                    name="talk"
                    placeholder="Enter their Talk"
                    required
                  ></textarea>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Submit Button -->
        <div class="d-grid gap-2 mt-4">
          <div class="text-center">
            <button type="submit" class="btn btn-primary btn-md">
              Create Testimonial
            </button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>
